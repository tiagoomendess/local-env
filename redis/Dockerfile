FROM redis:7-alpine

# Copy custom Redis configuration if needed
# COPY redis.conf /usr/local/etc/redis/redis.conf

# Redis with AOF persistence is already enabled via command in docker-compose

EXPOSE 6379
